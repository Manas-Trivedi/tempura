services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: tempura
      POSTGRES_PASSWORD: tempura
      POSTGRES_DB: tempura
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: